# V1.1.0 版本回归测试执行计划

> **版本：** v1.1.0（待发布）  
> **计划日期：** 2026-07-23  
> **上一版本：** v1.0.0  
> **提交范围：** `v1.0.0..HEAD`（15 个提交）  
> **覆盖范围：** SPEC 0007/0008/0009/0010/0011/0012 + V1.0 回归  
> **当前测试基线：** 后端 704 passed + 前端 411 passed = 1115 个测试

---

## 一、测试目标

在 V1.1.0 发布前，确认以下三点：

1. **新增功能无回归**：V1.1.0 的 6 个 SPEC 新增功能正常工作，不破坏 V1.0 已有能力。
2. **V1.0 主链路完好**：从创建项目到下载 Word/PPT 的完整端到端流程仍可跑通。
3. **质量门禁通过**：后端 0 warnings、前端 lint/build 通过、数据库迁移无错误。

---

## 二、测试环境准备

### 2.1 环境要求

- Python 3.10+（当前 3.13.5）
- Node.js 18+
- SQLite（Python 内置）
- DeepSeek API Key（可选，用于 SPEC 0007 真实 LLM 回归；无 Key 时走 LocalRule 降级）

### 2.2 环境初始化

```bash
# 后端
cd server
.venv\Scripts\activate
pip install -e ".[dev]"
.venv\Scripts\python.exe -m alembic upgrade head

# 前端
cd apps/web
npm install
```

### 2.3 环境变量（默认 LocalRule 模式，无需 API Key）

```bash
# 如需测试真实 DeepSeek 回归，配置以下变量
# $env:DEEPSEEK_API_KEY = "sk-xxxxxxxxxxxxxxxxxxxxxxxx"
# $env:REQUIREMENT_DRAFT_PROVIDER = "deepseek"
# $env:EVIDENCE_CARD_PROVIDER = "deepseek"
# $env:ANALYSIS_PLAN_PROVIDER = "deepseek"
# $env:CODE_TASK_PROVIDER = "deepseek"
# $env:OUTLINE_PROVIDER = "deepseek"
```

---

## 三、自动化测试套件（第一道门禁）

### 3.1 后端全量测试

**命令：**
```bash
cd server
.venv\Scripts\python.exe -m pytest -q
```

**预期结果：**
- `704 passed`
- `0 warnings`
- 无 skipped、无 xfail

**验收标准：** 任何测试失败或 warning 出现均阻断发布。

### 3.2 前端全量测试

**命令：**
```bash
cd apps/web
npm.cmd test -- --run
```

**预期结果：**
- `411 passed`
- 19 个测试文件全部通过

**验收标准：** 任何测试失败均阻断发布。

### 3.3 前端类型检查

**命令：**
```bash
cd apps/web
npm.cmd run lint
```

**预期结果：** TypeScript 严格类型检查通过，无错误。

### 3.4 前端构建

**命令：**
```bash
cd apps/web
npm.cmd run build
```

**预期结果：** Vite 构建通过，生成 `dist/`，无构建错误。

### 3.5 数据库迁移验证

**命令：**
```bash
cd server
.venv\Scripts\python.exe -m alembic upgrade head
```

**预期结果：** 迁移到 `0007`（V1.1.0 无新增迁移，确认现有迁移无错误）。

**可选：全新临时数据库验证**
```bash
# 使用全新 SQLite 文件验证迁移可从零执行
$env:DATABASE_URL = "sqlite:///D:/tmp/v1.1.0-verify.db"
.venv\Scripts\python.exe -m alembic upgrade head
```

---

## 四、SPEC 专项回归（第二道门禁）

### 4.1 SPEC 0007：真实 DeepSeek LLM 接入

**测试范围：** 5 个 Provider 的 LLM 优先 + LocalRule 降级

| # | 场景 | 执行方式 | 预期 |
| --- | --- | --- | --- |
| 0007-1 | LLM 降级到 LocalRule | 不配置 `DEEPSEEK_API_KEY`，触发各 provider | 候选正常生成，降级日志出现 |
| 0007-2 | 真实 LLM 调用（可选） | 配置有效 API Key + provider=deepseek，触发生成 | DeepSeek 返回结构化候选，Pydantic 校验通过 |
| 0007-3 | LLM 超时降级 | 配置无效 `DEEPSEEK_BASE_URL`，触发生成 | 降级到 LocalRule，不阻断流程 |
| 0007-4 | 结构化输出校验 | 配置真实 Key，触发生成 | Pydantic 严格校验，非法输出被拒绝 |

**针对性测试命令：**
```bash
cd server
.venv\Scripts\python.exe -m pytest tests/test_deepseek_client.py tests/test_deepseek_providers.py -v
```
**预期：** 36 个测试全部通过。

### 4.2 SPEC 0008：部署文档与运维指南

**测试范围：** 根目录 `README.md` 可用性

| # | 场景 | 执行方式 | 预期 |
| --- | --- | --- | --- |
| 0008-1 | 从零启动验证 | 按 README 步骤在新环境执行后端/前端/Worker 启动 | 三个进程均正常启动 |
| 0008-2 | 环境变量清单完整 | 核对 README 环境变量表与 `config.py` property 一致 | 无遗漏 |
| 0008-3 | 排障章节可用 | 按 README 常见排障验证端口冲突/SQLite 锁定/Worker 不领取 | 排障建议有效 |

### 4.3 SPEC 0009：前端测试覆盖补全

**测试范围：** 8 个 API 模块 + 11 个 Workspace 组件

| # | 场景 | 执行方式 | 预期 |
| --- | --- | --- | --- |
| 0009-1 | API 层测试 | `npm.cmd test -- --run` 全量 | 8 个 API 模块测试通过 |
| 0009-2 | 组件测试 | `npm.cmd test -- --run` 全量 | 11 个 Workspace 组件测试通过 |
| 0009-3 | 状态门控逻辑 | 检查 ProjectDetailView 14 种状态中文标签 | 全部正确映射 |

**针对性测试命令：**
```bash
cd apps/web
npm.cmd test -- --run
```
**预期：** 411 passed。

### 4.4 SPEC 0010：Word 模板支持

**测试范围：** 模板 CRUD + 渲染器 + 降级策略 + Worker 接线

| # | 场景 | 执行方式 | 预期 |
| --- | --- | --- | --- |
| 0010-1 | 模板上传 | 上传 `.docx` 模板到项目 | SHA-256 哈希存储，返回模板元信息 |
| 0010-2 | 模板覆盖 | 同项目重新上传模板 | 旧模板被覆盖，文件系统替换 |
| 0010-3 | 模板渲染 | 上传含 `{{project_name}}` 和 `{{#sections}}` 的模板后生成 Word | 占位符替换，章节循环渲染 |
| 0010-4 | 无模板降级 | 未上传模板时生成 Word | 使用默认模板，`template_used=false` |
| 0010-5 | 模板缺失降级 | 模板记录存在但文件缺失 | 降级到默认渲染，`WORD_TEMPLATE_FILE_MISSING` |
| 0010-6 | 模板解析失败 | 上传损坏的 `.docx` | 降级到默认渲染，`WORD_TEMPLATE_PARSE_FAILED` |
| 0010-7 | 模板下载 | 下载已上传模板 | 返回 `.docx` 文件流 |
| 0010-8 | 模板删除 | 删除项目模板 | 记录和文件均删除 |

**针对性测试命令：**
```bash
cd server
.venv\Scripts\python.exe -m pytest tests/test_word_template.py tests/test_outlines_api.py -v
```
**预期：** 模板相关 18 个测试全部通过。

### 4.5 SPEC 0011：PPT 配置选项

**测试范围：** 页数控制 + 主题色 + 图表开关 + 降级

| # | 场景 | 执行方式 | 预期 |
| --- | --- | --- | --- |
| 0011-1 | 无 body 生成 PPT | 不传 config 参数生成 PPT | 向后兼容，使用默认配置 |
| 0011-2 | 页数控制 | 配置 `target_slide_count=10` 生成 PPT | 实际页数接近 10（减去标题页和总结页） |
| 0011-3 | 页数下限校验 | 配置 `target_slide_count=4` | 返回 400 + 校验错误 |
| 0011-4 | 页数上限校验 | 配置 `target_slide_count=21` | 返回 400 + 校验错误 |
| 0011-5 | 预设主题色应用 | 配置 `theme_color="#1a73e8"` 生成 PPT | 标题 run 应用主题色 |
| 0011-6 | 无效主题色降级 | 配置 `theme_color="invalid"` | 降级到默认色，不报错 |
| 0011-7 | 图表全局开关 | 配置 `include_charts=false` | PPT 不包含图表页 |
| 0011-8 | 完整配置 | 配置页数+主题色+图表开关 | 三项配置全部生效 |

**针对性测试命令：**
```bash
cd server
.venv\Scripts\python.exe -m pytest tests/test_ppt_config.py -v
```
**预期：** 23 个测试全部通过。

### 4.6 SPEC 0012：数据保留周期配置

**测试范围：** 配置降级 + 清理脚本 + RUNNING 保护 + 级联删除

| # | 场景 | 执行方式 | 预期 |
| --- | --- | --- | --- |
| 0012-1 | 永久保留默认 | `DATA_RETENTION_DAYS=0` 运行清理脚本 | 报告"永久保留，退出"，不删除任何项目 |
| 0012-2 | dry-run 模式 | `DATA_RETENTION_DAYS=30` 运行 `--dry-run` | 输出过期项目报告，不实际删除 |
| 0012-3 | execute 模式 | `DATA_RETENTION_DAYS=30` 运行 `--execute` | 过期项目被删除（18 表 + 文件系统） |
| 0012-4 | dry-run + execute 同时指定 | 同时传 `--dry-run --execute` | 采用 dry-run（安全优先） |
| 0012-5 | 配置降级 | 设置负值/非数字/浮点数 | 降级到 0（永久保留） |
| 0012-6 | 活跃项目重置 | 项目 `updated_at` 在保留期内 | 不进入清理列表 |
| 0012-7 | RUNNING job 保护 | 过期项目有 PENDING/RUNNING 任务 | 跳过该项目，标记 `skipped` |
| 0012-8 | 终态 job 不保护 | 过期项目只有 SUCCEEDED/FAILED/CANCELLED | 正常清理 |
| 0012-9 | 级联删除完整性 | 清理一个含全链路数据的项目 | 18 张表记录 + 文件系统目录全部删除 |
| 0012-10 | 其他项目无影响 | 清理过期项目 A | 未过期项目 B 数据完整 |

**针对性测试命令：**
```bash
cd server
.venv\Scripts\python.exe -m pytest tests/test_data_retention_config.py tests/test_cleanup_safety.py tests/test_cleanup_expired_data.py tests/test_cleanup_script.py tests/test_cleanup_integration.py -v
```
**预期：** 58 个测试全部通过。

**手动脚本验证：**
```bash
cd server
.venv\Scripts\activate
# dry-run 验证（安全，不删除）
python -m scripts.cleanup_expired_data --dry-run
# 查看帮助
python -m scripts.cleanup_expired_data --help
```

---

## 五、V1.0 端到端主链路回归（第三道门禁）

### 5.1 完整状态机流转

**目标：** 确认 V1.1.0 改动未破坏 V1.0 的完整端到端流程。

| 步骤 | 操作 | 预期状态 | 预期结果 |
| --- | --- | --- | --- |
| 1 | 创建项目 | `DRAFT` | 项目创建成功，工作区目录生成 |
| 2 | 输入实验要求 | `REQUIREMENT_PARSED` | 任务单候选生成 |
| 3 | 确认任务单 | `REQUIREMENT_CONFIRMED` | L0-L3 分级正确 |
| 4 | 登记 URL 来源 | `SOURCES_COLLECTED`（手动触发 complete） | Source.status=FETCHED→PARSED |
| 5 | 生成证据卡片 | （Worker GENERATE_EVIDENCE） | EvidenceCard.status=CANDIDATE |
| 6 | 确认证据卡片 | `EVIDENCE_CONFIRMED` | 卡片状态 CONFIRMED |
| 7 | 上传数据集 | `DATASET_READY`（手动 complete） | 字段概览生成 |
| 8 | 生成分析方案 | `ANALYSIS_CONFIRMED` | 清洗+分析+图表方案 |
| 9 | 生成代码任务 | （Worker GENERATE_CODE_TASK） | CodeTask.status=CANDIDATE |
| 10 | 确认代码 + 执行 | `RESULT_CONFIRMED` | stdout/stderr/产物生成 |
| 11 | 生成大纲 | `OUTLINE_CONFIRMED` | 6 章节大纲 |
| 12 | 生成 Word + PPT | `COMPLETED` | 文件可下载 |

### 5.2 Worker 端到端验证脚本（可选）

```bash
cd server
.venv\Scripts\activate
python worker_e2e_verify.py
```

**预期：** 项目完整流转 `RESULT_CONFIRMED → COMPLETED`，Word 和 PPT 文件实际生成。

### 5.3 关键回归点

| # | 回归点 | 验证方式 | 预期 |
| --- | --- | --- | --- |
| R-1 | STALE 传播链 | 重新执行 ExecutionRun → 检查 Outline 状态 | Outline 变 STALE，Deliverable 变 STALE |
| R-2 | 交付物版本管理 | 同一 Outline 多次生成 Word | 旧版本保留，新版本递增 |
| R-3 | 失败不覆盖成功 | Word 生成失败后重新生成成功 | 失败版本保留，成功版本独立 |
| R-4 | Python 沙箱安全 | 执行含 `import socket` 的代码 | 被 AST 拦截，返回安全错误 |
| R-5 | URL 安全 | 登记 `localhost`/`file://` | 返回结构化错误，不采集 |
| R-6 | 路径穿越防护 | 下载 `../../../../etc/passwd` | 被拦截 |

---

## 六、验收标准总表

| 门禁 | 检查项 | 通过标准 | 阻断发布？ |
| --- | --- | --- | --- |
| 自动化-后端 | `pytest -q` | 704 passed, 0 warnings | 是 |
| 自动化-前端测试 | `npm test -- --run` | 411 passed | 是 |
| 自动化-前端 lint | `npm run lint` | TypeScript 通过 | 是 |
| 自动化-前端构建 | `npm run build` | Vite 构建通过 | 是 |
| 自动化-迁移 | `alembic upgrade head` | 无错误 | 是 |
| SPEC 0007 | LLM 接入 + 降级 | 5 provider 均可降级 | 是 |
| SPEC 0008 | 部署文档 | 从零启动可复现 | 是 |
| SPEC 0009 | 前端测试覆盖 | 8 API + 11 组件全覆盖 | 是 |
| SPEC 0010 | Word 模板 | CRUD + 渲染 + 降级 | 是 |
| SPEC 0011 | PPT 配置 | 页数 + 主题色 + 图表开关 | 是 |
| SPEC 0012 | 数据保留 | 清理脚本 + RUNNING 保护 | 是 |
| V1.0 回归 | 端到端主链路 | 创建→下载完整跑通 | 是 |
| V1.0 回归 | STALE 传播 | 传播链正确 | 是 |
| V1.0 回归 | 沙箱安全 | 禁止 import 拦截 | 是 |

---

## 七、执行顺序建议

```
1. 自动化测试套件（第三章节，5 项）
   ↓ 全部通过
2. SPEC 专项回归（第四章节，6 个 SPEC）
   ↓ 全部通过
3. V1.0 端到端主链路回归（第五章节）
   ↓ 全部通过
4. 生成 V1.1.0 端到端验收报告
   ↓
5. 项目负责人确认收口
   ↓
6. 打 tag v1.1.0 + push
```

---

## 八、已知限制与替代证据

| # | 限制 | 影响 | 替代证据 |
| --- | --- | --- | --- |
| L-1 | 当前会话未暴露 in-app Browser 工具 | 无法做真实浏览器点击截图验收 | 以 Vitest 组件测试 + API 测试套件作为替代 |
| L-2 | 真实 DeepSeek 调用需要有效 API Key | SPEC 0007 真实调用回归可选 | 不配置 Key 时走 LocalRule 降级，36 个 mock 测试覆盖 |
| L-3 | SPEC 0012 清理脚本 `--execute` 会删除真实数据 | 手动 execute 验证有风险 | dry-run 验证为主，execute 用测试内存数据库验证 |

---

## 九、回归测试执行记录

> 本节在执行时填写，记录实际命令和结果。

| 日期 | 检查项 | 命令 | 结果 | 状态 |
| --- | --- | --- | --- | --- |
| 2026-07-23 | 后端全量测试 | `pytest -q` | 704 passed, 0 warnings | ✅ |
| 2026-07-23 | 前端全量测试 | `npm test -- --run` | 411 passed | ✅ |
| 2026-07-23 | 前端 lint | `npm run lint` | `tsc --noEmit` 通过，无类型错误 | ✅ |
| 2026-07-23 | 前端构建 | `npm run build` | Vite 构建通过，114 模块转换，`dist/` 394.96 kB，gzip 107.49 kB | ✅ |
| 2026-07-23 | 数据库迁移 | `alembic upgrade head` | 迁移到 0007 无错误 | ✅ |
| 2026-07-23 | SPEC 0012 测试 | `pytest tests/test_data_retention_config.py tests/test_cleanup_safety.py tests/test_cleanup_expired_data.py tests/test_cleanup_script.py tests/test_cleanup_integration.py -v` | 58 passed | ✅ |

### 第二道门禁：SPEC 专项回归

| 日期 | 检查项 | 命令 | 结果 | 状态 |
| --- | --- | --- | --- | --- |
| 2026-07-23 | SPEC 0007 LLM 接入 | `pytest tests/test_deepseek_client.py tests/test_deepseek_providers.py -v` | 36 passed in 7.48s（5 Provider LLM 优先 + LocalRule 降级） | ✅ |
| 2026-07-23 | SPEC 0010 Word 模板 | `pytest tests/test_word_template.py tests/test_outlines_api.py -v` | 39 passed in 3.29s（18 模板 + 21 大纲 API） | ✅ |
| 2026-07-23 | SPEC 0011 PPT 配置 | `pytest tests/test_ppt_config.py -v` | 23 passed in 2.56s（页数/主题色/图表开关/降级） | ✅ |

### 第三道门禁：V1.0 端到端主链路回归

| 日期 | 检查项 | 命令 | 结果 | 状态 |
| --- | --- | --- | --- | --- |
| 2026-07-23 | Worker 端到端主链路 | `python worker_e2e_verify.py`（临时数据库 + 临时数据目录） | 项目 `proj_f4d1ef5672c3` 从 RESULT_CONFIRMED → COMPLETED；大纲 6 章节（candidate_source=local_rule）；Word v1 SUCCEEDED 37033 bytes；PPT v1 SUCCEEDED 32231 bytes；Word/PPT 文件实际生成；E2E_RESULT=PASS | ✅ |
| 2026-07-23 | 关键回归点（STALE/沙箱/路径穿越等） | `pytest tests/ -k "stale or sandbox or socket or path_traversal or localhost or file_scheme or version" -v` | 63 passed in 7.62s，覆盖 R-1 STALE 传播链 / R-2 交付物版本管理 / R-3 失败不覆盖成功 / R-4 socket 拦截 / R-5 localhost/file:// 拒绝 / R-6 路径穿越防护 | ✅ |

**回归测试三道门禁全部通过，V1.1.0 发布后 V1.0 主链路完好。**

**说明：**
- worker_e2e_verify.py 首次运行因硬编码 ID（`task_e2e_001`/`run_e2e_001`）与 V1.0 验收残留数据冲突失败（UNIQUE constraint failed），改用临时数据库（`DATABASE_URL=sqlite:///.tmp/v1.1.0-e2e-verify.db` + `PROJECT_DATA_ROOT=.tmp/v1.1.0-e2e-data`）重跑通过。脚本硬编码 ID 是已知缺陷（非 V1.1.0 回归），记录为可记录债务。
- V1.1.0 回归日志见 [worker-e2e-log-v1.1.0-regression.md](worker-e2e-log-v1.1.0-regression.md)，V1.0 原始验收日志保留为 [worker-e2e-log.md](worker-e2e-log.md)。
- LocalRule 降级路径验证通过（未配置 DEEPSEEK_API_KEY，5 Provider 均走 local_rule 候选源），证明 V1.1.0 SPEC 0007 LLM 接入未破坏 V1.0 主链路。
