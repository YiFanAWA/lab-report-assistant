# V1.2.0 版本回归测试执行计划

> **版本：** v1.2.0（待发布）  
> **计划日期：** 2026-07-25  
> **上一版本：** v1.1.0  
> **提交范围：** `v1.1.0..HEAD`（4 个提交：`27c7583`、`1f139d4`、`32b96c6`、`c210911` 为 SPEC 0013；`31ec6cd` 为 SPEC 0014；`5186a64`、`64f2eb4`、`e203ac2` 为 SPEC 0015）  
> **覆盖范围：** SPEC 0013/0014/0015 + V1.1.0 回归  
> **当前测试基线：** 后端 729 passed + 前端 411 passed = 1140 个测试

---

## 一、测试目标

在 V1.2.0 发布前，确认以下三点：

1. **新增功能无回归**：V1.2.0 的 3 个 SPEC（Docker 化、LLM 缓存、CI 流水线）功能正常工作，不破坏 V1.1.0 已有能力。
2. **V1.1.0 主链路完好**：从创建项目到下载 Word/PPT 的完整端到端流程仍可跑通。
3. **质量门禁通过**：后端 0 warnings、前端 lint/build 通过、数据库迁移无错误、CI 流水线全绿。

---

## 二、测试环境准备

### 2.1 环境要求

- Python 3.10+（当前 3.13.5）
- Node.js 18+
- SQLite（Python 内置）
- Docker Desktop（SPEC 0013 验证用，已收口）
- DeepSeek API Key（可选，SPEC 0014 缓存验证用 mock 测试，无需真实 Key）

### 2.2 环境初始化

```bash
# 后端
cd server
.venv\Scripts\activate
pip install -e ".[dev]"
.venv\Scripts\python.exe -m alembic upgrade head

# 前端
cd apps/web
npm install
```

### 2.3 环境变量（V1.2.0 新增 SPEC 0014 缓存配置）

```bash
# SPEC 0014 LLM 缓存（默认关闭，启用后相同输入复用 LLM 返回）
$env:LLM_CACHE_ENABLED = "false"  # 默认值
$env:LLM_CACHE_TTL_SECONDS = "86400"  # 默认 1 天
$env:LLM_CACHE_DB_PATH = "data/llm_cache/llm_cache.db"  # 默认值
```

---

## 三、自动化测试套件（第一道门禁）

### 3.1 后端全量测试

**命令：**
```bash
cd server
.venv\Scripts\python.exe -m pytest -q
```

**预期结果：**
- `729 passed`（V1.1.0 704 + SPEC 0014 新增 25）
- `0 warnings`
- 无 skipped、无 xfail

**验收标准：** 任何测试失败或 warning 出现均阻断发布。

### 3.2 前端全量测试

**命令：**
```bash
cd apps/web
npm.cmd test -- --run
```

**预期结果：**
- `411 passed`
- 19 个测试文件全部通过

**验收标准：** 任何测试失败均阻断发布。

### 3.3 前端类型检查

**命令：**
```bash
cd apps/web
npm.cmd run lint
```

**预期结果：** TypeScript 严格类型检查通过，无错误。

### 3.4 前端构建

**命令：**
```bash
cd apps/web
npm.cmd run build
```

**预期结果：** Vite 构建通过，生成 `dist/`，无构建错误。

### 3.5 数据库迁移验证

**命令：**
```bash
cd server
# 全新临时 SQLite 文件
$env:DATABASE_URL = "sqlite:///./.tmp/v1.2.0-verify.db"
.venv\Scripts\python.exe -m alembic upgrade head
```

**预期结果：** 迁移到 `0007`（V1.2.0 无新增迁移，SPEC 0014 缓存表通过 `CREATE TABLE IF NOT EXISTS` 自动建表，不走 Alembic）。

---

## 四、SPEC 专项回归（第二道门禁）

### 4.1 SPEC 0013：Docker 化部署（已收口，引用验收）

**测试范围：** 多阶段镜像构建 + 三服务编排 + 数据持久化 + 沙箱安全

V1.2.0 发布时引用 SPEC 0013 验收记录（commit `c210911`）：

| AC # | 验收项 | 验收状态 |
| --- | --- | --- |
| AC-1 | 后端镜像构建（895MB） | ✅ 通过（标准调整为 < 1000MB） |
| AC-2 | 前端镜像构建（93.2MB < 100MB） | ✅ 通过 |
| AC-3 | .dockerignore 排除规则 | ✅ 通过 |
| AC-4 | 一键 `docker compose up -d` | ✅ 通过 |
| AC-5 | worker 启动顺序（backend healthy 后） | ✅ 通过 |
| AC-6 | 健康检查 `/health` | ✅ 通过 |
| AC-7 | 前端可访问 `GET /` | ✅ 通过 |
| AC-8 | API 代理 `GET /api/projects` | ✅ 通过 |
| AC-9 | 数据库持久化（down → up 数据保留） | ✅ 通过 |
| AC-10 | 项目数据持久化（命名卷） | ✅ 通过 |
| AC-11 | volume 隔离（`down -v` 清空数据） | ✅ 通过 |
| AC-12 | 后端测试（本地 venv 704 passed，.dockerignore 排除 tests） | ✅ 通过（标准调整） |
| AC-13 | 迁移自动执行（entrypoint.sh） | ✅ 通过 |
| AC-14 | LocalRule 降级（5 Provider） | ✅ 通过 |
| AC-15 | Worker 领取任务 | ✅ 通过 |
| AC-16 | AST 拦截（socket/requests/urllib） | ✅ 通过 |
| AC-17 | 内存监控（psutil） | ✅ 通过 |
| AC-18 | 超时限制（EXECUTION_TIMEOUT） | ✅ 通过 |

**V1.2.0 发布无需重新验证 Docker**，引用 SPEC 0013 收口证据。

### 4.2 SPEC 0014：LLM 调用缓存

**测试范围：** LLMCache 存储层 + DeepSeekClient 接入 + 缓存命中/未命中/过期/禁用/降级

**针对性测试命令：**
```bash
cd server
.venv\Scripts\python.exe -m pytest tests/test_llm_cache.py tests/test_deepseek_client.py -v
```

**预期：** 25 个测试全部通过（test_llm_cache 20 + test_deepseek_client 缓存接入 5）。

| AC # | 验收项 | 验证方式 |
| --- | --- | --- |
| AC-1 | LLMCache 存储层 | `llm_cache.py` 自动建表/读写/TTL/淘汰 |
| AC-2 | 缓存 key 稳定性 | 相同输入产生相同 key，字段顺序不影响 |
| AC-3 | DeepSeekClient 接入 | cache=None 零回归，注入后调用前查/调用后写 |
| AC-4 | 缓存命中跳过 HTTP | mock httpx 不被调用 |
| AC-5 | 缓存未命中调用 HTTP | 调用 HTTP 并写入缓存 |
| AC-6 | 过期缓存 | TTL 过期后重新调用 HTTP |
| AC-7 | 禁用缓存 | LLM_CACHE_ENABLED=false 时不查不写 |
| AC-8 | 写入失败降级 | 异常不抛错，仍返回 content |
| AC-9 | 配置降级 | 非法 TTL/ENABLED 值降级到默认 |
| AC-10 | 现有 API 合同不变 | 5 个 provider 响应结构不变 |
| AC-11 | 现有测试无回归 | 原 704 个测试全部通过 |
| AC-12 | 新增测试通过 | 25 个新增测试，0 warnings |
| AC-13 | Docker 环境变量注入 | `.env.example` 新增 3 个变量 |
| AC-14 | 文档回写 | acceptance/README/tech-debt 更新 |

### 4.3 SPEC 0015：GitHub Actions CI 流水线

**测试范围：** CI 工作流 + 触发条件 + 后端 Job + 前端 Job + Secrets 管理

**验证方式：** 通过 GitHub REST API 查询 CI 运行状态：

```bash
# 查询最新 CI 运行
curl -s "https://api.github.com/repos/YiFanAWA/lab-report-assistant/actions/runs?branch=master&per_page=5"
```

**预期：** CI Run #2 和 Run #3 均为 `completed` + `conclusion=success`。

| AC # | 验收项 | 验证方式 |
| --- | --- | --- |
| AC-1 | 工作流文件创建 | `.github/workflows/ci.yml` 存在，GitHub 能解析 |
| AC-2 | 触发条件 | push 到 master 触发 CI（Run #1/#2/#3 验证） |
| AC-3 | 后端 Job 通过 | Run #2/#3 backend job 全 6 步 success |
| AC-4 | 数据库迁移通过 | Run #2/#3 step "运行数据库迁移" success |
| AC-5 | 前端 Job 通过 | Run #2/#3 frontend job 全 6 步 success |
| AC-6 | 前端构建通过 | Run #2/#3 step "生产构建" success |
| AC-7 | 不修改业务代码 | SPEC 0015 §1.3 明确不修改业务代码 |
| AC-8 | 不使用 Secrets | ci.yml 无 secrets 引用 |
| AC-9 | 文档回写 | acceptance/README/tech-debt 更新 |
| AC-10 | 分支状态可见 | GitHub master 分支显示最新 CI 状态 |

### 4.4 SPEC 0015 openpyxl 依赖修复（TD-007）

**测试范围：** openpyxl 依赖在 CI 中正确生效

**验证方式：** CI Run #1（未修复）→ Run #2（修复后）状态对比：

| Run # | Commit | 状态 | 说明 |
| --- | --- | --- | --- |
| Run #1 | `5186a64` | failure | backend job exit code 2，test_dataset_parser.py 导入 openpyxl 失败 |
| Run #2 | `64f2eb4` | success | openpyxl>=3.1.0 补入 pyproject.toml，backend 729 passed in 33s |
| Run #3 | `e203ac2` | success | 最终文档回写 commit，CI 持续绿色，无回归 |

---

## 五、V1.1.0 端到端主链路回归（第三道门禁）

### 5.1 完整状态机流转

**目标：** 确认 V1.2.0 改动（Docker/缓存/CI）未破坏 V1.1.0 的完整端到端流程。

| 步骤 | 操作 | 预期状态 | 预期结果 |
| --- | --- | --- | --- |
| 1 | 创建项目 | `DRAFT` | 项目创建成功 |
| 2 | 输入实验要求 | `REQUIREMENT_PARSED` | 任务单候选生成 |
| 3 | 确认任务单 | `REQUIREMENT_CONFIRMED` | L0-L3 分级正确 |
| 4 | 登记 URL 来源 | `SOURCES_COLLECTED` | Source.status=FETCHED→PARSED |
| 5 | 生成证据卡片 | （Worker） | EvidenceCard.status=CANDIDATE |
| 6 | 确认证据卡片 | `EVIDENCE_CONFIRMED` | 卡片状态 CONFIRMED |
| 7 | 上传数据集 | `DATASET_READY` | 字段概览生成 |
| 8 | 生成分析方案 | `ANALYSIS_CONFIRMED` | 清洗+分析+图表方案 |
| 9 | 生成代码任务 | （Worker） | CodeTask.status=CANDIDATE |
| 10 | 确认代码 + 执行 | `RESULT_CONFIRMED` | stdout/stderr/产物生成 |
| 11 | 生成大纲 | `OUTLINE_CONFIRMED` | 6 章节大纲 |
| 12 | 生成 Word + PPT | `COMPLETED` | 文件可下载 |

### 5.2 Worker 端到端验证脚本

```bash
cd server
$env:DATABASE_URL = "sqlite:///./.tmp/v1.2.0-e2e-verify.db"
$env:PROJECT_DATA_ROOT = "./.tmp/v1.2.0-e2e-data"
.venv\Scripts\python.exe worker_e2e_verify.py
```

**预期：** 项目完整流转 `RESULT_CONFIRMED → COMPLETED`，Word 和 PPT 文件实际生成。

### 5.3 关键回归点

| # | 回归点 | 验证方式 | 预期 |
| --- | --- | --- | --- |
| R-1 | STALE 传播链 | `pytest -k "stale"` | Outline/Deliverable 变 STALE |
| R-2 | 交付物版本管理 | `pytest -k "version"` | 旧版本保留，新版本递增 |
| R-3 | 失败不覆盖成功 | `pytest -k "stale or version"` | 失败版本保留 |
| R-4 | Python 沙箱安全 | `pytest -k "socket or sandbox"` | 禁止 import 拦截 |
| R-5 | URL 安全 | `pytest -k "localhost or file_scheme"` | 拒绝 localhost/file:// |
| R-6 | 路径穿越防护 | `pytest -k "path_traversal"` | 被拦截 |

**针对性测试命令：**
```bash
cd server
.venv\Scripts\python.exe -m pytest tests/ -k "stale or sandbox or socket or path_traversal or localhost or file_scheme or version" -v
```

---

## 六、验收标准总表

| 门禁 | 检查项 | 通过标准 | 阻断发布？ |
| --- | --- | --- | --- |
| 自动化-后端 | `pytest -q` | 729 passed, 0 warnings | 是 |
| 自动化-前端测试 | `npm test -- --run` | 411 passed | 是 |
| 自动化-前端 lint | `npm run lint` | TypeScript 通过 | 是 |
| 自动化-前端构建 | `npm run build` | Vite 构建通过 | 是 |
| 自动化-迁移 | `alembic upgrade head`（临时 SQLite） | 无错误，迁移到 0007 | 是 |
| SPEC 0013 | Docker 化（已收口引用） | AC-1~18 全部通过 | 是（引用收口证据） |
| SPEC 0014 | LLM 缓存 + 降级 | 25 个新增测试通过 | 是 |
| SPEC 0015 | CI 流水线全绿 | Run #2/#3 均成功 | 是 |
| SPEC 0015 TD-007 | openpyxl 修复 | Run #2 backend 729 passed | 是 |
| V1.1.0 回归 | 端到端主链路 | E2E_RESULT=PASS | 是 |
| V1.1.0 回归 | STALE 传播 | 传播链正确 | 是 |
| V1.1.0 回归 | 沙箱安全 | 禁止 import 拦截 | 是 |

---

## 七、执行顺序建议

```
1. 自动化测试套件（第三章节，5 项）
   ↓ 全部通过
2. SPEC 专项回归（第四章节，3 个 SPEC + TD-007 修复）
   ↓ 全部通过
3. V1.1.0 端到端主链路回归（第五章节）
   ↓ 全部通过
4. 生成 V1.2.0 端到端验收报告
   ↓
5. 项目负责人确认收口
   ↓
6. 打 tag v1.2.0 + push
```

---

## 八、已知限制与替代证据

| # | 限制 | 影响 | 替代证据 |
| --- | --- | --- | --- |
| L-1 | 当前会话未暴露 in-app Browser 工具 | 无法做真实浏览器点击截图验收 | 以 Vitest 组件测试 + API 测试套件作为替代 |
| L-2 | 真实 DeepSeek 调用需要有效 API Key | SPEC 0014 真实缓存命中验证可选 | 不配置 Key 时走 LocalRule 降级，25 个 mock 测试覆盖 |
| L-3 | Docker 镜像构建耗时较长 | 重新构建需 5-10 分钟 | 引用 SPEC 0013 收口证据，不重新构建 |
| L-4 | worker_e2e_verify.py 硬编码日志标题为"V1.0" | 日志标题与版本不符 | 已手动修正日志标题，脚本缺陷作为可记录债务 |

---

## 九、回归测试执行记录

> 本节在执行时填写，记录实际命令和结果。

### 第一道门禁：自动化测试套件

| 日期 | 检查项 | 命令 | 结果 | 状态 |
| --- | --- | --- | --- | --- |
| 2026-07-25 | 后端全量测试 | `pytest -q` | **729 passed in 79.72s**, 0 warnings | ✅ |
| 2026-07-25 | 前端全量测试 | `npm test -- --run` | **411 passed**（19 个测试文件） | ✅ |
| 2026-07-25 | 前端 lint | `npm run lint` | `tsc --noEmit` 通过，无类型错误 | ✅ |
| 2026-07-25 | 前端构建 | `npm run build` | Vite 构建通过，114 模块转换，`dist/` 394.96 kB，gzip 107.49 kB | ✅ |
| 2026-07-25 | 数据库迁移 | `alembic upgrade head`（临时 SQLite `./.tmp/v1.2.0-verify.db`） | 迁移到 0007 无错误，7 个迁移（0001-0007）全部可从零执行 | ✅ |

### 第二道门禁：SPEC 专项回归

| 日期 | 检查项 | 命令/来源 | 结果 | 状态 |
| --- | --- | --- | --- | --- |
| 2026-07-25 | SPEC 0013 Docker | 引用 SPEC 0013 验收记录（commit `c210911`） | AC-1~18 全部通过 | ✅ |
| 2026-07-25 | SPEC 0014 LLM 缓存 | `pytest tests/test_llm_cache.py tests/test_deepseek_client.py -v` | 25 个测试全部通过（test_llm_cache 20 + test_deepseek_client 5） | ✅ |
| 2026-07-25 | SPEC 0015 CI 流水线 | GitHub REST API 查询 | Run #2（`64f2eb4`）和 Run #3（`e203ac2`）均 completed+success | ✅ |
| 2026-07-25 | SPEC 0015 TD-007 修复 | CI Run #1/#2/#3 状态对比 | Run #1 failure → Run #2/#3 success，openpyxl>=3.1.0 已生效 | ✅ |

### 第三道门禁：V1.1.0 端到端主链路回归

| 日期 | 检查项 | 命令 | 结果 | 状态 |
| --- | --- | --- | --- | --- |
| 2026-07-25 | Worker 端到端主链路 | `python worker_e2e_verify.py`（临时数据库 `./.tmp/v1.2.0-e2e-verify.db` + 临时数据目录 `./.tmp/v1.2.0-e2e-data`） | 项目 `proj_43779acfba88` 从 RESULT_CONFIRMED → COMPLETED；Word v1 SUCCEEDED 37033 bytes；PPT v1 SUCCEEDED 32231 bytes；Word/PPT 文件实际生成；E2E_RESULT=PASS | ✅ |
| 2026-07-25 | 关键回归点（STALE/沙箱/路径穿越等） | `pytest tests/ -k "stale or sandbox or socket or path_traversal or localhost or file_scheme or version" -v` | **63 passed, 666 deselected in 7.12s**，覆盖 R-1 STALE 传播链 / R-2 交付物版本管理 / R-3 失败不覆盖成功 / R-4 socket 拦截 / R-5 localhost/file:// 拒绝 / R-6 路径穿越防护 | ✅ |

**回归测试三道门禁全部通过，V1.2.0 发布后 V1.1.0 主链路完好。**

**说明：**
- V1.2.0 回归日志见 [worker-e2e-log-v1.2.0-regression.md](worker-e2e-log-v1.2.0-regression.md)，V1.1.0 回归日志保留为 [worker-e2e-log-v1.1.0-regression.md](worker-e2e-log-v1.1.0-regression.md)，V1.0 原始验收日志保留为 [worker-e2e-log.md](worker-e2e-log.md)。
- LocalRule 降级路径验证通过（未配置 DEEPSEEK_API_KEY，5 Provider 均走 local_rule 候选源），证明 V1.2.0 SPEC 0014 缓存接入未破坏 V1.1.0 主链路（LLM_CACHE_ENABLED=false，cache=None 零回归）。
- CI Run #3 在 `2026-07-25T01:24:12Z` 由最终文档回写 commit `e203ac2` 推送触发，进一步确认 CI 持续绿色，无回归。
- worker_e2e_verify.py 脚本硬编码日志标题为"V1.0 Worker 端到端验证日志"，已手动修正 V1.2.0 回归日志标题，脚本本身缺陷作为可记录债务（不阻断 V1.2.0 发布）。
